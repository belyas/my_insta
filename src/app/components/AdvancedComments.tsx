import React from 'react';
import Container from '@mui/material/Container';
import Typography from '@mui/material/Typography';

const AdvancedComments: React.FC = () => (
  <Container maxWidth="sm" sx={{ mt: 4 }}>
    <Typography variant="h4" gutterBottom>Advanced Comments</Typography>
    <Typography variant="body1">Stub for comments with image, sticker, and more.</Typography>
  </Container>
);

export default AdvancedComments;
