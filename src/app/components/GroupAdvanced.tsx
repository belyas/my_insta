import React from 'react';
import Container from '@mui/material/Container';
import Typography from '@mui/material/Typography';

const GroupAdvanced: React.FC = () => (
  <Container maxWidth="sm" sx={{ mt: 4 }}>
    <Typography variant="h4" gutterBottom>Advanced Groups</Typography>
    <Typography variant="body1">Stub for group avatar, invite, love, newest members, online status.</Typography>
  </Container>
);

export default GroupAdvanced;
