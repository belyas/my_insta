import React from 'react';
import Container from '@mui/material/Container';
import Typography from '@mui/material/Typography';

const FeedExplore: React.FC = () => (
  <Container maxWidth="sm" sx={{ mt: 4 }}>
    <Typography variant="h4" gutterBottom>Feed Explore</Typography>
    <Typography variant="body1">Stub for explore, trending hashtags, mutual likes/comments, theatre mode, advanced time ago.</Typography>
  </Container>
);

export default FeedExplore;
