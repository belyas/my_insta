import React from 'react';
import Container from '@mui/material/Container';
import Typography from '@mui/material/Typography';

const ShareTagBookmark: React.FC = () => (
  <Container maxWidth="sm" sx={{ mt: 4 }}>
    <Typography variant="h4" gutterBottom>Share, Tag, Bookmark</Typography>
    <Typography variant="body1">Stub for share, tag/untag, copy link, and bookmark features.</Typography>
  </Container>
);

export default ShareTagBookmark;
